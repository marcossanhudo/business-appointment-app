import React from "react";
import { ScrollView } from "react-native";

export const ScheduleSuccessPage = () => {
    return (
        <ScrollView>
            <p>Test</p>
        </ScrollView>
    )
}